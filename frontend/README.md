# RailOps Analyzer Frontend

React + TypeScript + Vite 前端。开发时通过 Vite 把 `/api` 代理到 `http://127.0.0.1:8000`。

```powershell
npm install
npm run dev
```

可用检查：`npm run test`、`npm run lint`、`npm run build`。

