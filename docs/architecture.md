# 工程架构

项目遵循页面层、前端服务层、API 层、领域服务层和仓储层的职责划分。

- 页面只处理路由、布局、交互与加载状态。
- 前端服务层封装 API、DTO 与错误处理。
- API 层负责校验、状态码与 DTO 转换。
- 领域模块实现纯函数算法，不依赖 HTTP Request 或具体页面。
- 仓储层只负责数据持久化与查询。

开发期由 Vite 和 FastAPI 分别运行，Vite 代理 `/api`。生产期将通过多阶段 Docker 构建，把 `frontend/dist` 交给 FastAPI 托管。

